# Implementation plan

## Steps

1. Add a `Banner` component under `src/components/`.
2. Wire the banner into the app shell layout.
3. Run `bun test` and confirm the home route snapshot or assertion passes.

## Validation

- `bun test`
- `bun run typecheck`
